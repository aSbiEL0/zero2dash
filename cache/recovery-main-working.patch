diff --git a/nasa-app/app.py b/nasa-app/app.py
index d137d2e..4536700 100644
--- a/nasa-app/app.py
+++ b/nasa-app/app.py
@@ -42,8 +42,14 @@ FONTS_DIR = SCRIPT_DIR / "fonts"
 COUNTRY_MAP_PATH = SCRIPT_DIR / "country_codes.json"
 LOCATION_CACHE_PATH = SCRIPT_DIR / "location_cache.json"
 CREW_CACHE_PATH = SCRIPT_DIR / "crew_cache.json"
-MAP_ASSET_PATH = ASSETS_DIR / "world-map.png"
-ERROR_ASSET_PATH = ASSETS_DIR / "nasa_error.png"
+WORLD_MAP_PATH = ASSETS_DIR / "world-map.png"
+LEGACY_ERROR_ASSET_PATH = ASSETS_DIR / "nasa_error.png"
+MAP_TEMPLATE_PATH = ASSETS_DIR / "location.png"
+MAP_STALE_TEMPLATE_PATH = ASSETS_DIR / "location-error.png"
+DETAILS_TEMPLATE_PATH = ASSETS_DIR / "details.png"
+CREW_TEMPLATE_PATH = ASSETS_DIR / "people.png"
+CREW_STALE_TEMPLATE_PATH = ASSETS_DIR / "people-error.png"
+ERROR_TEMPLATE_PATH = ASSETS_DIR / "error-background.png"
 
 FBDEV_DEFAULT = os.environ.get("FB_DEVICE", "/dev/fb1")
 WIDTH_DEFAULT = int(os.environ.get("FB_WIDTH", "320"))
@@ -277,10 +283,10 @@ def generate_error_image(path: Path, width: int = CANVAS_WIDTH, height: int = CA
 
 
 def ensure_assets() -> None:
-    if not MAP_ASSET_PATH.exists():
-        generate_world_map(MAP_ASSET_PATH)
-    if not ERROR_ASSET_PATH.exists():
-        generate_error_image(ERROR_ASSET_PATH)
+    if not WORLD_MAP_PATH.exists():
+        generate_world_map(WORLD_MAP_PATH)
+    if not LEGACY_ERROR_ASSET_PATH.exists() and not ERROR_TEMPLATE_PATH.exists():
+        generate_error_image(LEGACY_ERROR_ASSET_PATH)
 
 
 def load_country_map() -> dict[str, str]:
@@ -928,6 +934,27 @@ def load_asset(path: Path) -> Image.Image:
             return image.convert("RGB").resize((CANVAS_WIDTH, CANVAS_HEIGHT), RESAMPLING_LANCZOS)
     return Image.new("RGB", (CANVAS_WIDTH, CANVAS_HEIGHT), BACKGROUND_RGB)
 
+
+def load_asset_candidates(*paths: Path) -> Image.Image:
+    for path in paths:
+        if path.exists():
+            return load_asset(path)
+    return Image.new("RGB", (CANVAS_WIDTH, CANVAS_HEIGHT), BACKGROUND_RGB)
+
+
+def rounded_panel_mask(width: int, height: int, radius: int) -> Image.Image:
+    mask = Image.new("L", (width, height), 0)
+    draw = ImageDraw.Draw(mask)
+    draw.rounded_rectangle((0, 0, width - 1, height - 1), radius=radius, fill=255)
+    return mask
+
+
+def paste_rounded_panel(base: Image.Image, panel: Image.Image, box: tuple[int, int, int, int], *, radius: int) -> None:
+    width = max(1, box[2] - box[0])
+    height = max(1, box[3] - box[1])
+    panel_image = panel.resize((width, height), RESAMPLING_LANCZOS)
+    base.paste(panel_image, box[:2], rounded_panel_mask(width, height, radius))
+
 def map_point(latitude: float, longitude: float, box: tuple[int, int, int, int]) -> tuple[int, int]:
     left, top, right, bottom = box
     width = max(1, right - left)
@@ -944,16 +971,12 @@ def draw_badge(draw: ImageDraw.ImageDraw, box: tuple[int, int, int, int], label:
 
 
 def render_map_page(location: LocationSnapshot, *, stale: bool) -> Image.Image:
-    image = load_asset(MAP_ASSET_PATH)
+    image = load_asset_candidates(MAP_STALE_TEMPLATE_PATH if stale else MAP_TEMPLATE_PATH, MAP_TEMPLATE_PATH, ERROR_TEMPLATE_PATH)
+    world_map = load_asset_candidates(WORLD_MAP_PATH)
+    map_box = (8, 28, CANVAS_WIDTH - 10, CANVAS_HEIGHT - 8)
+    paste_rounded_panel(image, world_map, map_box, radius=20)
     draw = ImageDraw.Draw(image)
-    header_box = (8, 8, CANVAS_WIDTH - 8, 32)
-    footer_box = (8, CANVAS_HEIGHT - 28, CANVAS_WIDTH - 8, CANVAS_HEIGHT - 8)
-    draw.rounded_rectangle(header_box, radius=10, fill=(7, 17, 31))
-    draw.rounded_rectangle(footer_box, radius=10, fill=(7, 17, 31))
-    title_font = load_font(14, bold=True)
     meta_font = load_font(12, bold=False)
-    draw.text((16, centred_text_y(title_font, "ISS now", 20)), "ISS now", font=title_font, fill=TEXT_RGB)
-    map_box = (10, 36, CANVAS_WIDTH - 10, CANVAS_HEIGHT - 34)
     if location.trail:
         previous = None
         for point in location.trail:
@@ -962,85 +985,88 @@ def render_map_page(location: LocationSnapshot, *, stale: bool) -> Image.Image:
                 draw.line((previous[0], previous[1], current[0], current[1]), fill=(252, 222, 110), width=2)
             previous = current
     marker_x, marker_y = map_point(location.latitude, location.longitude, map_box)
-    draw.ellipse((marker_x - 4, marker_y - 4, marker_x + 4, marker_y + 4), fill=(255, 94, 94), outline=(255, 244, 244))
-    draw.text((16, centred_text_y(meta_font, format_timestamp(location.position_timestamp), CANVAS_HEIGHT - 18)), format_timestamp(location.position_timestamp), font=meta_font, fill=MUTED_RGB)
-    if stale:
-        draw_badge(draw, (CANVAS_WIDTH - 90, 10, CANVAS_WIDTH - 18, 28), "STALE", fill=WARNING_RGB, text_fill=(24, 18, 10))
+    draw.ellipse((marker_x - 5, marker_y - 5, marker_x + 5, marker_y + 5), fill=(255, 94, 94), outline=(255, 244, 244), width=1)
+    timestamp_text = format_timestamp(location.position_timestamp)
+    draw.text((16, CANVAS_HEIGHT - 20), timestamp_text, font=meta_font, fill=(225, 225, 230))
     return image
 
 
 def render_details_page(location: LocationSnapshot, observer: ObserverConfig, *, stale: bool) -> Image.Image:
-    image = Image.new("RGB", (CANVAS_WIDTH, CANVAS_HEIGHT), BACKGROUND_RGB)
+    image = load_asset_candidates(DETAILS_TEMPLATE_PATH, ERROR_TEMPLATE_PATH)
     draw = ImageDraw.Draw(image)
-    title_font = load_font(17, bold=True)
-    label_font = load_font(12, bold=True)
-    value_font = load_font(15, bold=False)
-    small_font = load_font(11, bold=False)
-    draw.rounded_rectangle((10, 10, CANVAS_WIDTH - 10, 40), radius=12, fill=(10, 24, 40))
-    draw.text((18, centred_text_y(title_font, "ISS details", 24)), "ISS details", font=title_font, fill=TEXT_RGB)
+    label_font = load_font(11, bold=True)
+    value_font = load_font(12, bold=False)
+    small_font = load_font(10, bold=False)
     if stale:
-        draw_badge(draw, (CANVAS_WIDTH - 90, 13, CANVAS_WIDTH - 18, 31), "STALE", fill=WARNING_RGB, text_fill=(24, 18, 10))
-
-    def draw_pair(row_index: int, left_label: str, left_value: str, right_label: str, right_value: str) -> None:
-        centre_y = 62 + (row_index * 34)
-        left_col = LAYOUT_HALF.left
-        right_col = LAYOUT_HALF.right
-        draw.text((left_col.left, centre_y - 12), left_label, font=label_font, fill=MUTED_RGB)
-        left_text, right_text = truncate_pair(left_value, right_value, left_font=value_font, right_font=value_font, left_width_limit=left_col.width - 10, right_width_limit=right_col.width - 18)
-        draw.text((left_col.left, centre_y + 2), left_text, font=value_font, fill=TEXT_RGB)
-        draw.text((right_col.left, centre_y - 12), right_label, font=label_font, fill=MUTED_RGB)
-        draw.text((right_col.left, centre_y + 2), right_text, font=value_font, fill=TEXT_RGB)
-
-    draw_pair(0, "Longitude", f"{location.longitude:.2f}", "Latitude", f"{location.latitude:.2f}")
-    draw_pair(1, "Velocity", f"{location.velocity_kmh:.0f} km/h" if location.velocity_kmh is not None else "Unknown", "Altitude", f"{location.altitude_km:.1f} km" if location.altitude_km is not None else "Unknown")
-    body_top = 136
-    full_width = CANVAS_WIDTH - 30
+        draw.text((23, 217), "API error - displaying cached data", font=small_font, fill=(255, 198, 148))
+
+    def draw_label_value(label: str, value: str, label_box: tuple[int, int, int, int], value_box: tuple[int, int, int, int]) -> None:
+        label_x = label_box[0]
+        label_y = centred_text_y(label_font, label, (label_box[1] + label_box[3]) // 2)
+        value_x = value_box[0]
+        value_y = centred_text_y(value_font, value, (value_box[1] + value_box[3]) // 2)
+        draw.text((label_x, label_y), label, font=label_font, fill=TEXT_RGB)
+        draw.text((value_x, value_y), ellipsize_text(value, value_font, value_box[2] - value_box[0]), font=value_font, fill=TEXT_RGB)
+
+    draw_label_value("Longitude:", f"{location.longitude:.5f}", (35, 44, 155, 64), (182, 44, 297, 64))
+    draw_label_value("Latitude:", f"{location.latitude:.5f}", (40, 70, 145, 90), (176, 70, 297, 90))
     country_value = location.country_name or location.location_label or "International Waters"
-    draw.text((15, body_top), "Country", font=label_font, fill=MUTED_RGB)
-    draw.text((15, body_top + 16), ellipsize_text(country_value, value_font, full_width), font=value_font, fill=TEXT_RGB)
-    draw.text((15, body_top + 44), "Next flyover", font=label_font, fill=MUTED_RGB)
+    country_left, country_right = truncate_pair("Currently", country_value, left_font=label_font, right_font=value_font, left_width_limit=112, right_width_limit=118)
+    draw.text((36, 96), country_left, font=label_font, fill=TEXT_RGB)
+    draw.text((36, 114), "over:", font=label_font, fill=TEXT_RGB)
+    draw.text((166, 104), country_right, font=value_font, fill=TEXT_RGB)
+    draw_label_value("Velocity", f"{location.velocity_kmh:.0f}km/h" if location.velocity_kmh is not None else "Unknown", (38, 132, 154, 166), (172, 138, 297, 160))
+    draw.text((38, 152), "(km/h):", font=label_font, fill=TEXT_RGB)
+    draw_label_value("Altitude:", f"{location.altitude_km:.0f}km" if location.altitude_km is not None else "Unknown", (36, 178, 150, 198), (194, 178, 297, 198))
     flyover_label = location.flyover_label
     if observer.lat is None or observer.lon is None:
         flyover_label = "Next flyover unavailable"
-    draw.text((15, body_top + 60), ellipsize_text(flyover_label, value_font, full_width), font=value_font, fill=TEXT_RGB)
-    draw.text((15, body_top + 88), "Last live update", font=label_font, fill=MUTED_RGB)
-    draw.text((15, body_top + 104), ellipsize_text(format_timestamp(location.position_timestamp), small_font, full_width), font=small_font, fill=TEXT_RGB)
+    draw.text((20, 204), "Next flyover:", font=label_font, fill=TEXT_RGB)
+    draw.text((173, 204), ellipsize_text(flyover_label, value_font, 120), font=value_font, fill=TEXT_RGB)
+    draw.text((20, 220), ellipsize_text(format_timestamp(location.position_timestamp), small_font, CANVAS_WIDTH - 40), font=small_font, fill=MUTED_RGB)
     return image
 
 
 def render_crew_page(crew_page: list[CrewMember], page_number: int, total_pages: int, *, stale: bool) -> Image.Image:
-    image = Image.new("RGB", (CANVAS_WIDTH, CANVAS_HEIGHT), (10, 18, 30))
+    image = load_asset_candidates(CREW_STALE_TEMPLATE_PATH if stale else CREW_TEMPLATE_PATH, CREW_TEMPLATE_PATH, ERROR_TEMPLATE_PATH)
     draw = ImageDraw.Draw(image)
-    title_font = load_font(17, bold=True)
     name_font = load_font(15, bold=True)
     detail_font = load_font(12, bold=False)
     tag_font = load_font(11, bold=False)
-    draw.rounded_rectangle((10, 10, CANVAS_WIDTH - 10, 40), radius=12, fill=(12, 26, 42))
-    header_text = f"Crew {page_number}/{total_pages}"
-    draw.text((18, centred_text_y(title_font, header_text, 24)), header_text, font=title_font, fill=TEXT_RGB)
+    header_text = f"{page_number}/{total_pages}"
+    draw.text((283, 8), header_text, font=tag_font, fill=TEXT_RGB)
     if stale:
-        draw_badge(draw, (CANVAS_WIDTH - 90, 13, CANVAS_WIDTH - 18, 31), "STALE", fill=WARNING_RGB, text_fill=(24, 18, 10))
+        draw.text((23, 217), "API error - displaying cached data", font=detail_font, fill=(255, 198, 148))
     if not crew_page:
         empty_font = load_font(15, bold=False)
-        draw.text((22, 102), "Crew data unavailable", font=empty_font, fill=TEXT_RGB)
-        draw.text((22, 126), "No live data and no cache.", font=detail_font, fill=MUTED_RGB)
+        draw.text((28, 102), "Crew data unavailable", font=empty_font, fill=TEXT_RGB)
+        draw.text((28, 126), "No live data and no cache.", font=detail_font, fill=(210, 210, 216))
         return image
-    top = 54
+    top = 46
     for item in crew_page:
-        draw.rounded_rectangle((14, top, CANVAS_WIDTH - 14, top + 54), radius=12, fill=(16, 30, 46), outline=(30, 48, 72))
-        draw.text((24, top + 8), ellipsize_text(item.name, name_font, CANVAS_WIDTH - 74), font=name_font, fill=TEXT_RGB)
-        draw.text((24, top + 30), ellipsize_text(item.secondary, detail_font, CANVAS_WIDTH - 90), font=detail_font, fill=MUTED_RGB)
+        draw.rounded_rectangle((18, top, CANVAS_WIDTH - 18, top + 54), radius=12, fill=(17, 25, 48), outline=(60, 78, 110))
+        draw.text((28, top + 8), ellipsize_text(item.name, name_font, CANVAS_WIDTH - 88), font=name_font, fill=TEXT_RGB)
+        draw.text((28, top + 30), ellipsize_text(item.secondary, detail_font, CANVAS_WIDTH - 106), font=detail_font, fill=(214, 214, 220))
         if item.agency:
             badge_text = ellipsize_text(item.agency, tag_font, 72)
-            badge_left = CANVAS_WIDTH - 84
-            draw.rounded_rectangle((badge_left, top + 9, CANVAS_WIDTH - 24, top + 27), radius=8, fill=(33, 50, 72))
+            badge_left = CANVAS_WIDTH - 90
+            draw.rounded_rectangle((badge_left, top + 9, CANVAS_WIDTH - 28, top + 27), radius=8, fill=(33, 50, 72))
             draw.text((badge_left + 7, centred_text_y(tag_font, badge_text, top + 18)), badge_text, font=tag_font, fill=TEXT_RGB)
         top += 60
     return image
 
 
 def render_error_page() -> Image.Image:
-    return load_asset(ERROR_ASSET_PATH)
+    image = load_asset_candidates(ERROR_TEMPLATE_PATH, LEGACY_ERROR_ASSET_PATH)
+    draw = ImageDraw.Draw(image)
+    title_font = load_font(16, bold=True)
+    body_font = load_font(12, bold=False)
+    draw.rounded_rectangle((34, 82, CANVAS_WIDTH - 34, 176), radius=16, fill=(14, 18, 38), outline=(237, 174, 82), width=2)
+    draw.text((60, 98), "Startup failed", font=title_font, fill=(255, 222, 182))
+    draw.text((49, 122), "No live ISS data and no usable", font=body_font, fill=TEXT_RGB)
+    draw.text((64, 140), "cache was found.", font=body_font, fill=TEXT_RGB)
+    draw.text((91, 159), "Retry later.", font=body_font, fill=(255, 198, 148))
+    return image
 
 
 def build_pages(location: LocationSnapshot | None, crew_snapshot: CrewSnapshot | None, observer: ObserverConfig, *, map_stale: bool, details_stale: bool, crew_stale: bool) -> list[PageState]:
